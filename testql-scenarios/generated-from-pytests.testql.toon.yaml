# SCENARIO: Auto-generated from Python Tests
# TYPE: integration
# GENERATED: false

CONFIG[1]{key, value}:
  timeout_ms, 30000

# Execute each focused cleanup regression module through TestQL. Keeping one
# path per SHELL also works with older TestQL tokenizers.
SHELL "python -m pytest -q tests/unit/test_cleanup_cmd.py" 30000
ASSERT_EXIT_CODE 0
ASSERT_STDOUT_CONTAINS "passed"

SHELL "python -m pytest -q tests/unit/test_service_cleanup.py" 30000
ASSERT_EXIT_CODE 0
ASSERT_STDOUT_CONTAINS "passed"

SHELL "python -m pytest -q tests/unit/test_service_scanner.py" 30000
ASSERT_EXIT_CODE 0
ASSERT_STDOUT_CONTAINS "passed"
