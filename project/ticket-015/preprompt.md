# Ticket preprompt

- **Task ID**: ticket-015
- **Task title**: Show exact remediation effects and prefer GLM 5.3
- **Created**: 2026-09-01T14:30:53Z

Keep executable implementation outside this governance/evidence directory.
Read a human-owned user-*.md file only when one exists.
The request to execute this work creates SESSION_EXECUTION_AUTHORIZATION;
proceed within the recorded intent without a redundant confirmation prompt.
Require new authority for destructive action, secrets, external coordination,
material objective expansion and trusted merge approval.

Implementation focus from the authorized request:
- display exact affected paths/resources and commands before each remediation;
- prevent diagnostic bracket numbers from becoming executable actions;
- prefer the official OpenRouter `z-ai/glm-5.3` model identifier;
- preserve confirmation before every command and never run system cleanup as
  part of repository validation.
